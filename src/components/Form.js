import React from 'react';

const Form = () => {
   return (
      <form>
         <input 
            type="search"
            placeholder="Search character..."
         />
      </form>
   );
};

export default Form;